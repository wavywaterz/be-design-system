Text field with stacked label / hint / error; focus = ink border + honey ring.

```jsx
<Input label="Email" placeholder="you@example.com" hint="We write rarely." />
<Input label="Name" error="That didn't take. Try again." />
```
