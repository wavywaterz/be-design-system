Circular icon-only button (close, more, share); `label` is the accessible name and tooltip.

```jsx
<IconButton label="Close" variant="ghost" onClick={close}><XIcon/></IconButton>
```

Variants solid (honey) / soft (tint, default) / ghost / outline. Sizes 28/36/44.
