Surface container: white `default` (border + warm shadow-2), `tint` nectar flat, `inverse` ink band, `outline` transparent.

```jsx
<Card><h3>Tend</h3><p>Small acts, daily.</p></Card>
<Card variant="tint" padding={32}>…</Card>
```
